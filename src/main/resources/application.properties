spring.application.name=Jira
springdoc.swagger-ui.use-root-path=true

# =====================================================================
# 1. Local MySQL Database Connection Settings (Commented Out)
# =====================================================================
 #spring.datasource.url=jdbc:mysql://127.0.0.1:3306/jira?useSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true
 #spring.datasource.username=root
 #spring.datasource.password=root
 spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
 spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# RDS Config
spring.datasource.url=jdbc:mysql://jira.cnwvmiywwxpr.ap-south-1.rds.amazonaws.com:3306/jira
spring.datasource.username=root
spring.datasource.password=rootPassword

# =====================================================================
# 2. PostgreSQL Connection Settings (Neon Cloud DB - Active)
# =====================================================================
#spring.datasource.url=jdbc:postgresql://ep-lively-violet-ax57ygh4.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require
#spring.datasource.username=neondb_owner
#spring.datasource.password=npg_U8k3IzRXDMCF
#spring.datasource.driver-class-name=org.postgresql.Driver

# =====================================================================
# 3. Hibernate / JPA Properties
# =====================================================================
# Auto-updates database tables based on your JPA Entities
spring.jpa.hibernate.ddl-auto=update

# Enable SQL logging to the console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# =====================================================================
# 4. Cache Configuration (Caffeine)
# =====================================================================
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=500,expireAfterAccess=10m

# =====================================================================
# 5. Security / OAuth2
# =====================================================================
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://accounts.google.com